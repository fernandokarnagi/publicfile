try {
var matches = [];
var elems = document.getElementsByTagName("*");
for (var i=0; i<elems.length; i++) {
  if (elems[i].id.indexOf("Change_Request_ID") >= 0)
    elems[i].disabled = "disabled";
}
} catch (e) {}


